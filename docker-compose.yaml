version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/app
      - /app/node_modules
    ports:
      - "3000:8080"
    environment:
      - NODE_ENV=development
    command: npm run dev
